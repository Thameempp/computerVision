### Important pattern to remember

cv.resize(image, (width, height))
 
image[y_start:y_end, x_start:x_end]
 
cv.rotate(image, rotation_type)
 
cv.flip(image, direction)
 
cv.cvtColor(image, conversion_type)